启动区的名称可以是任意字符串（8字符）
每个扇区（sector）的大小（必须为512字节）
簇（cluster）的大小（必须为一个扇区）
FAT的起始位置（一般从第一个扇区开始）
FAT的个数（必须为2）
根目录的大小（通常为224项）
该磁盘的大小（必须为2880扇区）
磁盘的种类（必须为0xf0）
FAT的长度（必须为9扇区）
1个磁道（track）有几个扇区（必须是18）
磁头数（必须为2）
不使用分区（必须是0）
重写一次磁盘大小
意义不明，固定
（可能是）卷标号码
磁盘名称（11字节）
磁盘格式名称
先空出18字节